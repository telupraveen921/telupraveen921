class Solution {
    String firstAlphabet(String S) {
        String d=S.charAt(0)+"";
        for(int i=0;i<S.length()-1;i++){
            if((S.charAt(i)+"").equals(" ")){
                d=d+S.charAt(i+1);
            }
        }
        return d;
    }
};
